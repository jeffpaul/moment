{
    "name": "jeffpaul/moment",
    "description": "Moment — mobile-first WordPress publishing prototype. Dev dependencies for testing only.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require-dev": {
        "phpunit/phpunit": "^9.6",
        "yoast/phpunit-polyfills": "^2.0"
    },
    "scripts": {
        "test": "phpunit",
        "test:coverage": "phpunit --coverage-text",
        "phpcs": "echo 'PHPCS not configured yet — install wp-coding-standards/wpcs and add phpcs.xml.dist to enable.'"
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    }
}
